#!/bin/sh

#IP=127.0.0.1
IP=${1}
USER=gqiao

# if [ ! -f ~/.ssh/id_rsa.pub ]; then
#     ssh-keygen -t rsa 
# fi
# ssh-copy-id -i ~/.ssh/id_rsa.pub ${IP}

rsync -avz ${USER}@${IP}:${PWD}/ ${PWD}/







