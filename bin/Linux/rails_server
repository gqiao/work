#!/bin/sh

if pgrep "ruby"; then
    ps aux  | grep -m 1 "rails server"
else
    cd ${SRC_DIR}
    rails server&
    cd -
fi

google-chrome localhost:3000/$@

#tail -f ${SRC_DIR}/log/development.log

